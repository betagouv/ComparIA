# Keep Railway uploads lean: exclude local dev artefacts the images never need.
# IMPORTANT: do NOT use a blanket *.png here. frontend/static/** contains real PNG
# assets (logos, flags, home images); a global *.png makes them 404 on the site.
# Only the stray screenshots that live at the repo root are excluded, anchored with /.
/sante-*.png
/vote-*.png
/debug-state.png
/post-vote.png
.playwright-mcp/
docs/
.venv/
node_modules/
