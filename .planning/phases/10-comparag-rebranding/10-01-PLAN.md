---
phase: 10-comparag-rebranding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/locales/messages/fr.json
  - frontend/locales/messages/en.json
  - frontend/locales/messages/da.json
  - frontend/locales/messages/sv.json
  - frontend/locales/messages/nb_NO.json
  - frontend/locales/messages/lt.json
  - frontend/locales/messages/et.json
  - frontend/locales/messages/es.json
  - frontend/src/lib/components/header/Header.svelte
  - frontend/src/lib/components/Newsletter.svelte
  - frontend/src/lib/logger.server.ts
  - frontend/src/routes/(pages)/ranking/+page.svelte
  - frontend/src/routes/(pages)/ranking/components/Methodology.svelte
  - frontend/src/error.html
autonomous: false
requirements:
  - REBRAND-01
  - REBRAND-02
  - REBRAND-03
  - REBRAND-04

must_haves:
  truths:
    - "Site header reads CompaRAG, not compar:IA"
    - "Hero section describes MCP tool comparison, not LLM comparison"
    - "All nav tabs describe CompaRAG-specific content"
    - "No visible compar:IA string remains on any deployed page"
    - "Download filenames reference comparag, not comparia"
    - "Error page message references CompaRAG"
  artifacts:
    - path: "frontend/locales/messages/fr.json"
      provides: "French locale — 47 occurrences replaced"
    - path: "frontend/locales/messages/en.json"
      provides: "English locale — 31 occurrences replaced"
    - path: "frontend/locales/messages/da.json"
      provides: "Danish locale — 14 occurrences replaced"
    - path: "frontend/locales/messages/sv.json"
      provides: "Swedish locale — 23 occurrences replaced"
    - path: "frontend/locales/messages/nb_NO.json"
      provides: "Norwegian locale — 16 occurrences replaced"
    - path: "frontend/src/lib/components/Newsletter.svelte"
      provides: "Newsletter title updated"
    - path: "frontend/src/lib/logger.server.ts"
      provides: "Log tag updated to comparag-frontend"
    - path: "frontend/src/error.html"
      provides: "Error message updated"
  key_links:
    - from: "frontend/locales/messages/fr.json"
      to: "deployed site French copy"
      via: "Paraglide i18n runtime"
      pattern: "compar:IA"
    - from: "frontend/src/error.html"
      to: "visible error page"
      via: "SvelteKit error boundary"
      pattern: "compar:IA"
---

<objective>
Replace all compar:IA branding with CompaRAG across every locale JSON and the 6 hardcoded Svelte/TS/HTML files.

Purpose: The site is being rebranded from an LLM comparison tool (compar:IA) to an MCP tool comparison platform (CompaRAG). All visible copy must reflect the new brand and purpose before any further feature work.

Output: 8 locale JSON files and 6 source files where every user-visible string references "CompaRAG" and describes MCP tool comparison rather than LLM comparison.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-comparag-rebranding/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite all locale JSON files</name>
  <files>
    frontend/locales/messages/fr.json,
    frontend/locales/messages/en.json,
    frontend/locales/messages/da.json,
    frontend/locales/messages/sv.json,
    frontend/locales/messages/nb_NO.json,
    frontend/locales/messages/lt.json,
    frontend/locales/messages/et.json,
    frontend/locales/messages/es.json
  </files>
  <action>
    Process all 8 locale JSON files. For each file:

    STEP 1 — Mechanical replacement (all files):
    Replace every occurrence of `compar:IA` with `CompaRAG` exactly.
    Replace every occurrence of `comparIA` (no colon, camelCase) with `CompaRAG`.
    Do NOT touch: external URLs (huggingface.co/comparIA, digitalpublicgoods.net), localStorage key strings ('comparia-cohorts', 'comparia:tos') — but these should not appear in locale JSON anyway.

    STEP 2 — Contextual rewrite of LLM-specific copy (fr.json and en.json primarily, propagate to others):
    Find keys that describe the product purpose and rewrite them for MCP tool comparison context.

    Rewriting rules:
    - "compare des modèles de langage" / "compare language models" → "compare des outils MCP" / "compare MCP tools"
    - "IA" used as abbreviation for the product → "CompaRAG"
    - Hero headline: should convey "blind comparison of MCP servers on the same task"
    - Hero subtitle: should convey the equifinality concept — same task, same goal, different tools, you judge which wins
    - Any description mentioning "LLM", "grand modèle de langage", "intelligence artificielle" as the subject being compared → replace with "MCP tool", "MCP server", "outil MCP"
    - Arena/duel page descriptions: frame the contest as two MCP tools competing on the same query
    - Navigation tabs: rewrite descriptions that explain the purpose in LLM terms to MCP terms

    Do NOT rewrite: FAQ section strings (deferred per D-05). If a key starts with `faq.` or contains FAQ content, leave it untouched.
    Do NOT rewrite: toolArena section strings (already CompaRAG-native per D-06).
    Do NOT change: JSON structure, key names, Paraglide variable syntax ({variable}), or HTML tags inside values.

    STEP 3 — Verify valid JSON after edits:
    Each file must remain parseable JSON. No trailing commas, no broken quotes.
  </action>
  <verify>
    <automated>cd frontend && for f in locales/messages/fr.json locales/messages/en.json locales/messages/da.json locales/messages/sv.json locales/messages/nb_NO.json locales/messages/lt.json locales/messages/et.json locales/messages/es.json; do python3 -c "import json; json.load(open('$f'))" && echo "$f: valid JSON" || echo "$f: INVALID JSON"; done && grep -c "compar:IA\|comparIA" locales/messages/fr.json locales/messages/en.json locales/messages/da.json locales/messages/sv.json locales/messages/nb_NO.json || true</automated>
  </verify>
  <done>
    All 8 locale files parse as valid JSON. grep -c returns 0 for all files.
    fr.json and en.json hero/subtitle keys describe MCP tool comparison, not LLM comparison.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix hardcoded references in Svelte/TS/HTML files</name>
  <files>
    frontend/src/lib/components/header/Header.svelte,
    frontend/src/lib/components/Newsletter.svelte,
    frontend/src/lib/logger.server.ts,
    frontend/src/routes/(pages)/ranking/+page.svelte,
    frontend/src/routes/(pages)/ranking/components/Methodology.svelte,
    frontend/src/error.html
  </files>
  <action>
    Apply the following targeted edits (one per line — do not touch anything else in these files):

    **Header.svelte (line ~78):**
    - Keep `src="/orgs/comparia.png"` unchanged (no new asset yet — per D-04)
    - Change `alt` attribute on that img tag to `alt="CompaRAG logo"`

    **Newsletter.svelte (line ~31):**
    - Change `title="Infolettre compar:IA"` to `title="CompaRAG Newsletter"`
    - If there is adjacent visible text "compar:IA" in the same component, replace it with "CompaRAG"

    **logger.server.ts (line ~17):**
    - Change `app: 'comparia-frontend'` to `app: 'comparag-frontend'`
    - This is a log metadata tag; no other changes in this file

    **ranking/+page.svelte (line ~75):**
    - Change download filename prefix `comparia_model-` to `comparag_model-`
    - The commented-out line (~112) with `comparia_model-preferences-` should also be updated for consistency, even though it is commented out

    **ranking/components/Methodology.svelte (line ~51):**
    - Change download filename prefix `comparia_model-winrate-` to `comparag_model-winrate-`

    **src/error.html (line ~269):**
    - Change `Le service compar:IA rencontre un problème` to `Le service CompaRAG rencontre un problème`
    - Do not alter any other HTML structure in this file

    DO NOT change:
    - `COHORT_STORAGE_KEY = 'comparia-cohorts'` in cohortStore.svelte.ts (D-02)
    - `comparia.beta.gouv.fr` domain references in global.svelte.ts (external domain, not in scope)
    - Any `comparia:tos` string (session key, D-02)
  </action>
  <verify>
    <automated>grep -n "compar:IA\|comparia-frontend\|comparia_model\|Infolettre compar" frontend/src/lib/components/header/Header.svelte frontend/src/lib/components/Newsletter.svelte frontend/src/lib/logger.server.ts "frontend/src/routes/(pages)/ranking/+page.svelte" "frontend/src/routes/(pages)/ranking/components/Methodology.svelte" frontend/src/error.html; echo "exit $?"</automated>
  </verify>
  <done>
    The grep above returns zero matches. Each of the 6 files has been updated exactly as specified.
    cohortStore.svelte.ts and global.svelte.ts are unchanged (verify with git diff --name-only).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verify on comparag-beta.vercel.app</name>
  <what-built>
    All locale JSON files and 6 hardcoded source files have been updated. The full rebranding is complete at the code level. A commit has been made and pushed to trigger a Vercel deploy.
  </what-built>
  <how-to-verify>
    1. Wait for Vercel deploy to complete (check https://vercel.com dashboard or watch for deploy notification)
    2. Open https://comparag-beta.vercel.app in a browser
    3. Check each of the following:
       - [ ] Page title / browser tab reads "CompaRAG" (not "compar:IA")
       - [ ] Site header logo alt text is "CompaRAG logo" (inspect element)
       - [ ] Hero headline and subtitle describe MCP tool comparison (not LLM comparison)
       - [ ] Navigate to all main nav tabs — none should show "compar:IA" copy
       - [ ] Switch language to English — same checks pass
       - [ ] Navigate to the ranking page, download a file — filename starts with `comparag_model-`
       - [ ] Trigger an error (or check the static error.html if accessible) — message says "CompaRAG" not "compar:IA"
       - [ ] Open browser DevTools > Application > Local Storage — verify keys are still named `comparia-cohorts` and `comparia:tos` (unchanged — existing sessions preserved)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any remaining issues to fix</resume-signal>
</task>

</tasks>

<verification>
After Task 1 and Task 2 complete (before checkpoint):

```bash
# Zero compar:IA occurrences in locale files
grep -rl "compar:IA\|comparIA" frontend/locales/messages/

# Zero compar:IA occurrences in the 6 hardcoded files
grep -rn "compar:IA\|Infolettre compar\|comparia-frontend\|comparia_model" \
  frontend/src/lib/components/header/Header.svelte \
  frontend/src/lib/components/Newsletter.svelte \
  frontend/src/lib/logger.server.ts \
  "frontend/src/routes/(pages)/ranking/" \
  frontend/src/error.html

# localStorage keys preserved (must still be present)
grep "comparia-cohorts\|comparia:tos" frontend/src/lib/stores/cohortStore.svelte.ts
```

All three commands must confirm: locale files clean, hardcoded files clean, session keys intact.
</verification>

<success_criteria>
1. `grep -rl "compar:IA" frontend/locales/messages/` returns empty
2. `grep -rl "comparIA" frontend/locales/messages/` returns empty
3. All 8 locale JSON files pass `python3 -c "import json; json.load(open(f))"` without error
4. The 6 hardcoded files contain no remaining `compar:IA` / `Infolettre compar` / `comparia-frontend` / `comparia_model` strings
5. `COHORT_STORAGE_KEY = 'comparia-cohorts'` is unchanged in cohortStore.svelte.ts
6. Human checkpoint approved: comparag-beta.vercel.app shows CompaRAG branding throughout
</success_criteria>

<output>
After completion, create `.planning/phases/10-comparag-rebranding/10-01-SUMMARY.md` following the standard summary template.
</output>
