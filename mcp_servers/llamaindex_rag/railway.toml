[build]
builder = "DOCKERFILE"
dockerfilePath = "mcp_servers/llamaindex_rag/Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
